#include "monty.h"

/**
 * nop -  it does nothing
 * @stack: for the stack
 * @line_count : for nothing
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_count)
{
	(void) stack;
	(void) line_count;
}
